import { Metadata } from 'next';
import FormsClient from './Client';
export const metadata: Metadata = { title: 'نماذج التسجيل | المجرة الحضارية' };
export default function FormsPage() { return <FormsClient />; }
